server {
    listen 80;
    server_name moodle.au-team.irpo;
    
    # Директивы для отладки
    access_log /var/log/nginx/moodle.access.log;
    error_log /var/log/nginx/moodle.error.log warn;

    location / {
        proxy_pass http://10.1.1.1:80/;  # Явно указываем порт 80
        proxy_set_header Host            $host;
        proxy_set_header X-Real-IP       $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Критичные настройки для стабильности
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
        send_timeout 60s;
    }
}

server {
    listen 80;
    server_name wiki.au-team.irpo;
    
    # Директивы для отладки
    access_log /var/log/nginx/wiki.access.log;
    error_log /var/log/nginx/wiki.error.log warn;

    location / {
        proxy_pass http://10.2.2.1:8080;
        proxy_set_header Host            $host;
        proxy_set_header X-Real-IP       $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Критичные настройки для стабильности
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
        send_timeout 60s;
    }
}
